name: 'sixthsense_proj' # profiles.yml에 정의된 프로필 이름과 일치해야 함
version: '1.0.0'
config-version: 2

profile: 'sixthsense_proj' # profiles.yml의 프로필 이름

model-paths: ["models"]
macro-paths: ["macros"]

models:
  sixthsense_proj:
    # 모든 모델에 대한 기본 설정 (테스트 시 사용 가능)
    # +materialized: ephemeral

    staging:
      +materialized: view
    marts:
      +materialized: table